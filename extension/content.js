chrome.runtime.onMessage.addListener(function(message, sender, sendResponse) { if (message.action === 'extractMetadata') { try { var url = window.location.href; var title = document.title; var timestamp = new Date().toLocaleString(); var fullText = document.body ? document.body.innerText : ''; var textSnippet = fullText.replace(/\s+/g, ' ').trim().slice(0, 500); sendResponse({ url: url, title: title, timestamp: timestamp, textSnippet: textSnippet }); } catch(err) { sendResponse({ error: err.message }); } return true; } });
